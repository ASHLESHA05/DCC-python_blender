# 1. Inventory Display: Create a UI that shows the inventory from the database. 
# 2. Purchase/Return: Add buttons to buy/return items (updates the database and the DCC plugin's display). 
# 3. Responsiveness: The UI should not freeze while waiting for server responses.
